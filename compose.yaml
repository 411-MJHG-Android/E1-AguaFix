services:
  postgres:
    image: postgis/postgis:16-master
    container_name: 511-aguafix-db
    ports:
      - 5433:5432
    environment:
      - POSTGRES_PASSWORD=aguafix123
      - POSTGRES_DB=aguafixdb
    volumes:
      - postgres_data_db:/var/lib/postgresql

 
volumes:
  postgres_data_db:
